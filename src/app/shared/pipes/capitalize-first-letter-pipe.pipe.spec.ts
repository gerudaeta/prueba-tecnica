import { CapitalizeFirstLetterPipePipe } from './capitalize-first-letter-pipe.pipe';

describe('CapitalizeFirstLetterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalizeFirstLetterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
